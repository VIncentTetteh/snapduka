import Link from "next/link";import {createClient} from "@supabase/supabase-js";
export const dynamic="force-dynamic";
export default async function DiscoverPage({searchParams}:{searchParams:Promise<{q?:string;country?:string}>}){const filters=await searchParams;const client=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,{auth:{persistSession:false}});let query=client.from("discovery_listings").select("shop_id,slug,display_name,country,category,city,description,quality_score").eq("active",true).order("quality_score",{ascending:false}).order("shop_id").limit(100);if(filters.country)query=query.eq("country",filters.country);if(filters.q)query=query.or(`display_name.ilike.%${filters.q}%,category.ilike.%${filters.q}%,city.ilike.%${filters.q}%`);const{data}=await query;return <main className="mx-auto grid w-full max-w-5xl gap-4 px-3 py-8"><header><p className="font-bold uppercase text-emerald-900">Optional discovery</p><h1 className="text-5xl font-black">Find independent shops</h1></header><form className="flex flex-wrap gap-2"><input className="min-h-12 flex-1 rounded-xl border p-3" defaultValue={filters.q} name="q" placeholder="Search shops, categories or cities"/><select className="min-h-12 rounded-xl border p-3" defaultValue={filters.country??""} name="country"><option value="">All countries</option><option value="GH">Ghana</option><option value="NG">Nigeria</option><option value="CI">Côte d’Ivoire</option></select><button className="primaryAction">Search</button></form><section className="grid gap-3 sm:grid-cols-2">{data?.map(shop=><Link className="rounded-3xl border bg-white p-5 no-underline" href={`/${shop.slug}?campaign=discovery`} key={shop.shop_id}><h2>{shop.display_name}</h2><p>{shop.category} · {shop.city} · {shop.country}</p><p>{shop.description}</p></Link>)}</section></main>}
