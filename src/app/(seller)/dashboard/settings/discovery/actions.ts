"use server";import {revalidatePath} from "next/cache";import {resolveServerActor} from "@/lib/auth/actor";import {createClient} from "@/lib/supabase/server";
export async function saveDiscovery(formData:FormData){const actor=await resolveServerActor();if(actor.kind!=="seller")return;const supabase=await createClient();const{data:shop}=await supabase.from("shops").select("id").eq("seller_account_id",actor.sellerAccountId).single();if(!shop)return;await supabase.from("discovery_preferences").upsert({shop_id:shop.id,seller_account_id:actor.sellerAccountId,opted_in:formData.get("optedIn")==="on",category:String(formData.get("category")).trim()||null,city:String(formData.get("city")).trim()||null,description:String(formData.get("description")).trim()||null});await supabase.rpc("refresh_discovery_listing",{p_shop_id:shop.id});revalidatePath("/dashboard/settings/discovery");revalidatePath("/discover")}
