"use server";import {createHash,randomBytes} from "node:crypto";import {revalidatePath} from "next/cache";import {resolveServerActor} from "@/lib/auth/actor";import {createClient} from "@/lib/supabase/server";
export async function inviteTeamMember(formData:FormData){const actor=await resolveServerActor();if(actor.kind!=="seller"||actor.role)return;const email=String(formData.get("email")).trim().toLowerCase();const role=String(formData.get("role"));if(!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email)||!["manager","catalog","fulfillment","support","analyst"].includes(role))return;const token=randomBytes(32).toString("hex");const supabase=await createClient();await supabase.from("team_invitations").insert({seller_account_id:actor.sellerAccountId,email,role,token_hash:createHash("sha256").update(token).digest("hex"),invited_by:actor.userId,expires_at:new Date(Date.now()+7*86_400_000).toISOString()});revalidatePath("/dashboard/settings/team")}
export async function revokeTeamMember(formData:FormData){const actor=await resolveServerActor();if(actor.kind!=="seller"||actor.role)return;const supabase=await createClient();await supabase.from("team_memberships").update({active:false,revoked_at:new Date().toISOString()}).eq("id",String(formData.get("membershipId"))).eq("seller_account_id",actor.sellerAccountId);revalidatePath("/dashboard/settings/team")}
