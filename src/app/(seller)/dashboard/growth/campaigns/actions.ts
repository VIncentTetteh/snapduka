"use server";
import {revalidatePath} from "next/cache";import {resolveServerActor} from "@/lib/auth/actor";import {normalizeCampaignToken} from "@/lib/campaigns/links";import {createClient} from "@/lib/supabase/server";
export async function createCampaign(formData:FormData){const actor=await resolveServerActor();if(actor.kind!=="seller")return;const supabase=await createClient();const{data:shop}=await supabase.from("shops").select("id").eq("seller_account_id",actor.sellerAccountId).single();if(!shop)return;const name=String(formData.get("name")).trim();const channel=String(formData.get("channel"));if(!name||!["snapchat","tiktok","instagram","whatsapp","other"].includes(channel))return;await supabase.from("campaign_links").insert({seller_account_id:actor.sellerAccountId,shop_id:shop.id,name,token:`${normalizeCampaignToken(name)}-${crypto.randomUUID().slice(0,6)}`,channel});revalidatePath("/dashboard/growth/campaigns")}
