import Link from "next/link";import {resolveServerActor} from "@/lib/auth/actor";import {createClient} from "@/lib/supabase/server";
export default async function CustomersPage(){const actor=await resolveServerActor();if(actor.kind!=="seller")return null;const supabase=await createClient();const{data}=await supabase.from("customers").select("id,name,email,phone,orders(total_minor,created_at)").eq("seller_account_id",actor.sellerAccountId).order("updated_at",{ascending:false});return <main className="mx-auto grid w-full max-w-4xl gap-4 px-3 py-5 pb-24"><header><h1 className="text-4xl font-black">Customers</h1><p>Seller-scoped purchase history and consent records.</p></header>{data?.map(customer=>{const orders=customer.orders??[];return <Link className="rounded-2xl border bg-white p-4 no-underline" href={`/dashboard/customers/${customer.id}`} key={customer.id}><strong>{customer.name}</strong><p>{customer.email} · {orders.length} orders · {orders.reduce((sum,o)=>sum+o.total_minor,0)} minor units</p></Link>})}</main>}
