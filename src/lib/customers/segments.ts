/**
 * Segment matching lives in @snapduka/core so a segment previewed in the seller
 * app contains the same people the marketing worker later sends to.
 */
export {
  isEmptyRule,
  matchesSegment,
  type CustomerAggregate,
  type SegmentRule,
} from "@snapduka/core";
